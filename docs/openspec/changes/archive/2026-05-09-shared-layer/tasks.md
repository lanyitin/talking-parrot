## 1. Package scaffolding

- [x] 1.1 [P] Create `src/talking_parrot/shared/__init__.py` exposing the public API symbols (`ProjectSnapshot`, `AudioInfo`, `SnapshotLoader`, `FileSnapshotLoader`, `ScoreCard`, `MetricBundle`, `CueDiff`); behavior: `from talking_parrot.shared import ...` imports succeed for each symbol. Verified by a test `tests/unit/shared/test_public_api.py::test_all_symbols_importable` that imports each name from the package.
- [x] 1.2 [P] Create `tests/unit/shared/__init__.py` so pytest discovers the new test package; verified by `uv run pytest tests/unit/shared --collect-only` listing the new test files.

## 2. ProjectSnapshot value object (Reuse existing model classes by reference, do not re-define them; `ProjectSnapshot` is a separate frozen dataclass, not an extension of `ProjectFile`)

- [x] 2.1 Implement the `AudioInfo value object` as a frozen dataclass with fields `sample_rate: int`, `duration_ms: int`, `rms_mean: float`, `rms_peak: float` in `src/talking_parrot/shared/project_snapshot.py`. Behavior: attempting to reassign any field after construction raises `dataclasses.FrozenInstanceError`. Verified by `tests/unit/shared/test_project_snapshot.py::test_audio_info_is_frozen`.
- [x] 2.2 Implement the `ProjectSnapshot frozen aggregate` as a frozen dataclass with the field set defined in `specs/project-snapshot/spec.md`, importing `RawVadFrame`, `VadSegment`, `Chunk`, `TranscriptionResult`, `Subtitle` from `src/talking_parrot/models/` to satisfy `ProjectSnapshot reuses existing pipeline value objects`. Behavior: list fields default to `[]`; mutation raises `dataclasses.FrozenInstanceError`. Verified by `tests/unit/shared/test_project_snapshot.py::test_default_lists_empty` and `::test_snapshot_is_frozen`.
- [x] 2.3 Implement the `from_project_file bridge` classmethod on `ProjectSnapshot` that copies `vad_segments`, `transcription_results`, and `subtitles` from a `ProjectFile` and accepts keyword arguments for `vad_frames`, `chunks`, `pre_postprocess_subtitles`. Behavior: round-tripping a `ProjectFile` carrying three `TranscriptionResult` entries yields a snapshot with the same three entries in order. Verified by `tests/unit/shared/test_project_snapshot.py::test_from_project_file_round_trip`.
- [x] 2.4 Confirm type identity is preserved: a `Subtitle` constructed via `talking_parrot.models.subtitle.Subtitle` placed into `ProjectSnapshot.subtitles` passes `isinstance(..., Subtitle)`. Verified by `tests/unit/shared/test_project_snapshot.py::test_subtitle_type_identity`.

## 3. SnapshotLoader (Use a Protocol, not an ABC, for the loader abstraction; File-backed loader is the only implementation in this change)

- [x] 3.1 Implement the `SnapshotLoader protocol` in `src/talking_parrot/shared/snapshot_loader.py` as a `typing.Protocol` decorated with `@runtime_checkable`, declaring `load(source: str | Path) -> ProjectSnapshot`. Behavior: a duck-typed stub class with a `load(self, source)` method passes `isinstance(stub, SnapshotLoader)`. Verified by `tests/unit/shared/test_snapshot_loader.py::test_duck_typed_loader_satisfies_protocol`.
- [x] 3.2 Implement the `FileSnapshotLoader default implementation` whose `load(source)` reads JSON and constructs a `ProjectSnapshot`. Behavior: loading a well-formed `.tp` file returns a snapshot whose fields equal the file contents. Verified by `tests/unit/shared/test_snapshot_loader.py::test_load_well_formed_file`.
- [x] 3.3 Implement `Tolerant defaults for missing list fields`: each of `vad_frames`, `vad_segments`, `chunks`, `transcription_results`, `pre_postprocess_subtitles`, `subtitles` defaults to `[]` when absent, and a `logging.debug` message names each defaulted field. Verified by `tests/unit/shared/test_snapshot_loader.py::test_missing_list_defaults_empty` (uses `caplog` to assert the debug message names `vad_frames`).
- [x] 3.4 Implement `Hard failure on missing required scalar field`: missing `version`, `created_at`, `source_path`, `config_snapshot`, or `audio_info` raises `KeyError` whose message contains the missing field name. Verified by `tests/unit/shared/test_snapshot_loader.py::test_missing_required_raises_keyerror` parametrized over the five field names.
- [x] 3.5 Confirm `Underlying I/O errors propagate unchanged`: `FileNotFoundError` and `json.JSONDecodeError` from the underlying read are not wrapped or suppressed. Verified by `tests/unit/shared/test_snapshot_loader.py::test_missing_file_raises_filenotfound` and `::test_malformed_json_raises_decodeerror`.

## 4. Quality metrics value objects (`ScoreCard` and friends are empty-shape this change)

- [x] 4.1 [P] Implement the `MetricBundle value object` as a frozen dataclass with required fields `cer`, `confidence_mean`, `confidence_p10`, `repetition_ratio_mean`, `no_speech_prob_mean` (all `float`) in `src/talking_parrot/shared/metrics.py`. Behavior: construction requires all five fields; reassignment raises `FrozenInstanceError`. Verified by `tests/unit/shared/test_metrics.py::test_metric_bundle_required_fields` and `::test_metric_bundle_frozen`.
- [x] 4.2 [P] Implement the `CueDiff value object` as a frozen dataclass with fields `index`, `reference_text`, `hypothesis_text`, `cer`, `start_ms_delta`, `end_ms_delta`. Behavior: instances carry the supplied reference and hypothesis text. Verified by `tests/unit/shared/test_metrics.py::test_cue_diff_carries_reference_and_hypothesis`.
- [x] 4.3 Implement the `ScoreCard value object` as a frozen dataclass with fields `sample_id`, `overall_score`, `metric_bundle`, `cue_diffs` (defaulting to `[]`). Behavior: default `cue_diffs` is empty; mutation raises `FrozenInstanceError`. Verified by `tests/unit/shared/test_metrics.py::test_score_card_default_cue_diffs_empty` and `::test_score_card_frozen`.
- [x] 4.4 Confirm `No scoring logic in this capability`: enumerate the public symbols of `talking_parrot.shared.metrics` and assert each is one of the three dataclass types (or a typing import) — no callable producing scores is present. Verified by `tests/unit/shared/test_metrics.py::test_module_exposes_only_dataclasses`.

## 5. Quality gates

- [x] 5.1 [P] Add docstrings to every public module, class, and method introduced in `src/talking_parrot/shared/`; behavior: the project's docstring convention from CLAUDE.md is satisfied. Verified by manual content review and by `uv run ruff check src/talking_parrot/shared` reporting no `D` rule violations (if enabled) or by a content-review pass over the diff.
- [x] 5.2 Run `uv run pytest tests/unit/shared --tb=short` and confirm 100% green; behavior: the entire shared-layer test suite passes. Verified by command exit code 0.
- [x] 5.3 Run `uv run ruff check src/talking_parrot/shared tests/unit/shared` and `uv run ruff format --check src/talking_parrot/shared tests/unit/shared`; behavior: no lint or formatting issues remain. Verified by both commands exiting 0.
- [x] 5.4 Run `uv run mypy src/talking_parrot/shared` and confirm no type errors; behavior: the shared layer type-checks under strict configuration. Verified by command exit code 0.
